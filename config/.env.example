# Supabase 配置
# 从 Supabase 项目仪表板获取这些值: Settings > API
# 请复制此文件为 .env 并填入实际的值

# Supabase 项目 URL
# 格式: https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_URL=your-project-url.supabase.co

# Supabase 公开 API 密钥 (anon public key)
# 这是用于前端的公开密钥，可以安全地暴露在客户端代码中
VITE_SUPABASE_ANON_KEY=your-anon-key

# 重要提示：
# 1. 不要提交包含真实密钥的 .env 文件到 Git（已在 .gitignore 中）
# 2. 在本地开发时，复制此文件为 .env 并填入从 Supabase 获取的真实值
# 3. 在 Vercel 部署时，在项目设置的 Environment Variables 中添加这些变量
# 4. 详细设置步骤请参考 SUPABASE_SETUP_GUIDE.md

# 获取 Supabase 配置的步骤：
# 1. 登录 Supabase (https://supabase.com)
# 2. 选择你的项目
# 3. 进入 Settings > API
# 4. 复制 Project URL 和 Project API keys 中的 anon public key
# 5. 将这些值填入上面的环境变量中

# 部署到 Vercel 时的环境变量配置：
# 1. 在 Vercel 项目设置中进入 Environment Variables
# 2. 添加 VITE_SUPABASE_URL 变量，值为你的 Supabase 项目 URL
# 3. 添加 VITE_SUPABASE_ANON_KEY 变量，值为你的 Supabase anon key
# 4. 确保这些变量在 Production, Preview, 和 Development 环境中都已设置
