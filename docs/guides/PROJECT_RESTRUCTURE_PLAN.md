# 项目重构计划

## 🎯 目标
将当前散乱的文件结构重新组织，使项目更加清晰、易于维护和扩展。

## 📁 新的目录结构

```
ai-tools-navigation/
├── 📁 src/                          # 源代码目录
│   ├── 📁 js/                       # JavaScript 文件
│   │   ├── core/                    # 核心功能
│   │   │   ├── app.js              # 主应用
│   │   │   ├── config.js           # 配置管理
│   │   │   └── constants.js        # 常量定义
│   │   ├── modules/                 # 功能模块
│   │   │   ├── auth.js             # 认证模块
│   │   │   ├── tools.js            # 工具管理
│   │   │   ├── ui.js               # UI 管理
│   │   │   └── search.js           # 搜索功能
│   │   └── utils/                   # 工具函数
│   │       ├── validators.js        # 验证函数
│   │       ├── helpers.js          # 辅助函数
│   │       └── api.js              # API 封装
│   ├── 📁 css/                      # 样式文件
│   │   ├── main.css                # 主样式
│   │   ├── components.css          # 组件样式
│   │   ├── modals.css              # 模态框样式
│   │   └── responsive.css          # 响应式样式
│   └── 📁 assets/                   # 静态资源
│       ├── images/                  # 图片
│       └── icons/                   # 图标
├── 📁 docs/                         # 文档目录
│   ├── guides/                      # 指南文档
│   │   ├── deployment.md           # 部署指南
│   │   ├── authentication.md       # 认证指南
│   │   └── development.md          # 开发指南
│   ├── api/                         # API 文档
│   └── troubleshooting/             # 故障排除
├── 📁 tests/                        # 测试目录
│   ├── unit/                        # 单元测试
│   ├── integration/                 # 集成测试
│   ├── e2e/                         # 端到端测试
│   └── utils/                       # 测试工具
├── 📁 scripts/                      # 构建和部署脚本
│   ├── build.js                    # 构建脚本
│   ├── deploy.js                   # 部署脚本
│   └── dev-server.js               # 开发服务器
├── 📁 config/                       # 配置文件
│   ├── vercel.json                 # Vercel 配置
│   ├── .env.example                # 环境变量示例
│   └── supabase-setup.sql          # 数据库设置
├── 📁 public/                       # 公共文件（部署目录）
│   ├── index.html                  # 主页面
│   ├── debug-auth.html             # 调试页面
│   ├── favicon.ico                 # 网站图标
│   └── robots.txt                  # 搜索引擎配置
└── 📄 根目录文件
    ├── README.md                   # 项目说明
    ├── package.json                # 项目配置
    ├── .gitignore                  # Git 忽略文件
    └── CHANGELOG.md                # 更新日志
```

## 🔄 重构步骤

### 第一阶段：创建新目录结构
1. 创建 `src/` 目录及子目录
2. 创建 `docs/` 目录及子目录
3. 创建 `tests/` 目录及子目录
4. 创建 `scripts/` 目录
5. 创建 `config/` 目录
6. 创建 `public/` 目录

### 第二阶段：移动和重组文件
1. 移动 JavaScript 文件到 `src/js/` 对应目录
2. 拆分 `style.css` 到多个专门的 CSS 文件
3. 移动 HTML 文件到 `public/` 目录
4. 移动文档文件到 `docs/` 目录
5. 移动测试文件到 `tests/` 目录
6. 移动配置文件到 `config/` 目录

### 第三阶段：更新引用路径
1. 更新 HTML 文件中的资源引用路径
2. 更新 JavaScript 文件中的模块引用
3. 更新构建脚本中的路径配置
4. 更新 Vercel 配置

### 第四阶段：优化和清理
1. 删除重复和过时的文件
2. 合并相似功能的文件
3. 优化模块依赖关系
4. 更新文档和说明

## 📋 文件分类

### 🔧 核心源代码
- `js/app.js` → `src/js/core/app.js`
- `js/config.js` → `src/js/core/config.js`
- `js/auth.js` → `src/js/modules/auth.js`
- `js/tools.js` → `src/js/modules/tools.js`
- `js/ui.js` → `src/js/modules/ui.js`
- `script.js` → `src/js/modules/search.js`
- `style.css` → 拆分到 `src/css/` 多个文件

### 📄 HTML 页面
- `index.html` → `public/index.html`
- `debug-auth.html` → `public/debug-auth.html`
- `test-auth.html` → `tests/pages/auth-test.html`

### 📚 文档文件
- `README.md` → 保持根目录
- `*_GUIDE.md` → `docs/guides/`
- `*_SUMMARY.md` → `docs/summaries/`

### 🧪 测试文件
- `test-*.js` → `tests/unit/` 或 `tests/integration/`
- `test-*.html` → `tests/pages/`

### ⚙️ 配置文件
- `vercel.json` → `config/vercel.json`
- `.env.example` → `config/.env.example`
- `supabase-setup.sql` → `config/supabase-setup.sql`

### 🔨 构建脚本
- `build.js` → `scripts/build.js`

## 🎯 预期收益

1. **更清晰的结构** - 文件按功能分类，易于查找
2. **更好的维护性** - 相关文件集中管理
3. **更容易扩展** - 新功能有明确的放置位置
4. **更专业的外观** - 符合现代前端项目标准
5. **更好的协作** - 团队成员容易理解项目结构

## ⚠️ 注意事项

1. 重构过程中需要仔细更新所有文件引用
2. 确保 Vercel 部署配置正确指向新的文件位置
3. 保持向后兼容性，避免破坏现有功能
4. 逐步进行，每个阶段都要测试功能正常